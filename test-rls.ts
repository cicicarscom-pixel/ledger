import { createClient } from "npm:@supabase/supabase-js"; const supabase = createClient(Deno.env.get("SUPABASE_URL"), Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")); const { data } = await supabase.from("posts").select("profile_id, id").limit(5); console.log("Posts profile_ids:", data); const { data: m } = await supabase.from("organization_members").select("*").limit(5); console.log("Org members:", m);
